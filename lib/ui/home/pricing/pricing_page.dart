import 'package:flutter/material.dart';

class PricingPage extends StatelessWidget {
  PricingPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('pricing works like a charm')),
    );
  }
}
